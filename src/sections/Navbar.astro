---
import LanguageDropdown from "@/components/LanguageDropdown.astro";
import ThemeToggle from "@/components/ThemeToggle.astro";

const navItems = [
    {
        title: "Sobre mí",
        url: "/#sobre-mi",
    },
    {
        title: "Experiencia",
        url: "/#experiencia",
    },
    {
        title: "Stack",
        url: "/#stack",
    },
    {
        title: "Contacto",
        url: "/#contacto",
    },
]
---

<header class="fixed top-4 left-0 right-0 z-50 px-4">
    <div class="header-nav w-full mx-auto container lg:max-w-6xl md:max-w-4xl rounded-full py-3 px-6 shadow-lg flex items-center justify-between bg-slate-900/70 backdrop-blur-md border border-slate-700/50 text-slate-900 dark:text-slate-200">
        <a href="/" class="text-3xl font-bold tracking-wider text-slate-900 dark:text-slate-200 transition-colors">&lt;UCQ/&gt;</a>
        <div>
            <nav class="hidden md:flex items-center gap-6 lg:gap-8 text-sm font-medium">
                {
                    navItems.map((link) => (
                        <a
                            class="hover:text-white transition-colors"
                            href={link.url}
                        >
                            {link.title}
                        </a>
                    ))
                }
                <div class="pl-6 border-l border-gray-300/30 dark:border-white/10">
                    <ThemeToggle />
                </div>
            </nav>
            <div class="flex items-center">
                <button id="menu-toggle" class="md:hidden p-1 text-slate-400 hover:text-white focus:outline-none transition-colors" aria-label="Abrir menú">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-6 h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                    </svg>
                </button>
            </div>
        </div>
    </div>
    <div id="mobile-menu" class="fixed inset-y-0 left-0 z-50 w-full max-w-xs bg-slate-900/95 backdrop-blur-lg border-r border-slate-800 p-6 flex flex-col justify-between -translate-x-full transition-transform duration-300 ease-in-out md:hidden shadow-2xl">
        <div>
            <div class="flex items-center justify-between mb-6">
                <span class="text-2xl font-bold tracking-wider text-white">&lt;UCQ/&gt;</span>
                <button id="menu-close" class="p-1 text-slate-400 hover:text-white transition-colors" aria-label="Cerrar menú">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-6 h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            <hr class="mb-8 text-white">
            <nav class="flex flex-col gap-5 text-lg font-medium">
                {
                    navItems.map((link) => (
                        <a
                            class="mobile-link text-slate-300 hover:text-white py-1 transition-colors"
                            href={link.url}
                        >
                            {link.title}
                        </a>
                    ))
                }
            </nav>
        </div>
    </div>
    <div id="menu-overlay" class="fixed inset-0 bg-black/40 backdrop-blur-sm z-40 hidden md:hidden opacity-0 transition-opacity duration-300"></div>
</header>

<style>

    .header-nav {
        animation: nav-appear 1s linear both;
        animation-timeline: scroll();
        animation-range: 0 100px;
    }

    .dark body header .header-nav {
        animation: nav-appear-dark 1s linear both;
        animation-timeline: scroll();
        animation-range: 0 100px;
    }

    .header-nav {
        background-color: rgb(255 255 255 / 0.6);
    }

    .dark body header .header-nav {
        background-color: rgb(17 24 39 / 0.8);
    }

    @keyframes nav-appear {
        0% {
            background-color: transparent;
            border-color: transparent;
            box-shadow: none;
        }
        to {
            box-shadow: 0 0 0 1px rgb(255 255 255 / 0.1), 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgb(229 231 235 / 0.2);
        }
    }

    @keyframes nav-appear-dark {
        0% {
            background-color: rgb(17 24 39 / 0);
            border-color: transparent;
            box-shadow: none;
        }
        to {
            box-shadow: 0 0 0 1px rgb(255 255 255 / 0.05);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgb(255 255 255 / 0.05);
        }
    }

</style>

<script>

    const menuToggle = document.getElementById('menu-toggle');
    const menuClose = document.getElementById('menu-close');
    const mobileMenu = document.getElementById('mobile-menu');
    const menuOverlay = document.getElementById('menu-overlay');
    const mobileLinks = document.querySelectorAll('.mobile-link');
    
    function openMenu() {
        mobileMenu?.classList.remove('-translate-x-full');
        menuOverlay?.classList.remove('hidden');
        // Pequeño timeout para que la animación de opacidad del overlay funcione
        setTimeout(() => menuOverlay?.classList.add('opacity-100'), 10);
        document.body.style.overflow = 'hidden'; // Evita scroll al estar abierto
    }
    
    function closeMenu() {
        mobileMenu?.classList.add('-translate-x-full');
        menuOverlay?.classList.remove('opacity-100');
        setTimeout(() => menuOverlay?.classList.add('hidden'), 300);
        document.body.style.overflow = '';
    }
    
    // Event Listeners
    menuToggle?.addEventListener('click', openMenu);
    menuClose?.addEventListener('click', closeMenu);
    menuOverlay?.addEventListener('click', closeMenu);
    
    // Cerrar el menú automáticamente al hacer clic en cualquier sección
    mobileLinks.forEach(link => {
        link.addEventListener('click', closeMenu);
    });

</script>